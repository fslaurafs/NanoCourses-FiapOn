fun main(args: Array<String>) {
    // Ternário: Equivalente Kotlin (if condição "resultado se true" else "resultado se false")
    var grade = 7.5
    var result = if (grade > 7.0) "Aprovado" else "Reprovado"
    println(result)		// Aprovado
}
