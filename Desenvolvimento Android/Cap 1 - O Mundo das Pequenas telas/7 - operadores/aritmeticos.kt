fun main(args: Array<String>) {
    // Aritméticos (+, -, *, /, %)
    var a = 12
    var b = 3
    
    var sum = a + b					// 15
    var subtract = a - b			// 9
    var multiplication = a * b		// 36
    var division = a / b			// 4
    var modulus = a % b				// Resto da divisão: 0
    var minusA = -a					// -12
}
